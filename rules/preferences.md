# Personal Preferences

- Prefer concise responses. One sentence updates while working, 1–2 sentence summaries at end.
- Do not recap what you just did — I can read the diff.
- No trailing "Let me know if you need anything else" filler.
- Use markdown headers only when the response is long enough to warrant navigation.
- When editing code: prefer Edit over Write unless it's a full rewrite.
- Default git branch name: `master` (this machine uses the old default).
